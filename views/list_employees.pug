extends layout

block content
  .container
    .article-wrapper
      .col-md-6.center
        h3 Users
        ul.list-group
          each user in users
            li.list-group-item
              a(href='/users/'+user._id)= user.name